# tools/sorry_baseline.txt — declarations permitted to depend on `sorryAx`.
# Read by tools/audit_check.py (from tools/lint.sh). An entry here is an OPEN OBLIGATION, not a
# proof: the declaration type-checks but its proof term is incomplete. Delete the entry in the
# commit that closes it. One fully-qualified name per line; `#` starts a comment.
#
# The assembled Itô–Lévy formula (Epic B4). Its statement carries the hypotheses the assembly
# consumes — the cited result's, plus the corrections in tools/cited_axioms.md entry 16 — and its
# proof is the small-jump truncation, reduced to the six inputs of
# `JumpFormulaLimit.ae_itoLevy_of_ae_tendsto_integrals` plus the truncated data they range over.
# The seven `sorry`s in it are those obligations, each carrying its own local context; see the
# comments at each one in Ito/JumpFormulaAssembled.lean.
LevyStochCalc.Ito.JumpFormula.itoLevyFormula_jumpResidual_canonical

# The Itô–Lévy formula for a state function with bounded first and second derivatives (milestone
# M16, Epic B4). Its five limit obligations are discharged; the remaining `sorry` is the
# finite-activity identity along the truncated paths, in the mixed form — the derivatives read at
# the truncated path, the coefficients along the original one. See the `[OB-1]` comment in
# Ito/ItoLevyBoundedDerivs.lean. This theorem is NARROWER than the axiom it is a step towards:
# it does not close cited axiom 16, which stays open for a general C² state function.
LevyStochCalc.Ito.JumpFormula.itoLevyFormula_jumpResidual_of_boundedDerivs
